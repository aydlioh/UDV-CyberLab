import { lazy } from 'react';

export const MyProjectsPage = lazy(() => import('./MyProjectsPage'));
export const ProjectsPage = lazy(() => import('./ProjectsPage'));
export const ProjectPreviewPage = lazy(() => import('./ProjectPreviewPage'));
